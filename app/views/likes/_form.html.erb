  <div class="post-wrapper">
    <%= form_with(model: like, class: "contents") do |form| %>
   <%= form.hidden_field :post_id %>

    <%= form.submit "Like", class:"btn btn-outline-dark" %>

    <% end %>
  </div>
